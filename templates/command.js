module.exports = {
  name: "commandName",
  devOnly: false, //if only the developer should be able to use it or not (true if that should be the case)
  async execute(interaction) {
    //Execute Code
  }
}